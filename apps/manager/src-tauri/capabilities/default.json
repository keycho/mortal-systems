{
  "$schema": "https://schema.tauri.app/capabilities/2",
  "identifier": "default",
  "description": "main window: core defaults. the app-defined runtime_call command is not ACL-gated for local content (no app manifest is declared in build.rs), so it needs no entry here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
