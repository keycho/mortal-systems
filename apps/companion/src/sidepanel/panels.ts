import type { StampedConfig } from "../lib/config.js";

/**
 * day-4 scope: notes editor, ai context + local chat log, permissions with
 * enforcement badges. this stub keeps the day-3 side panel honest — nothing
 * renders that does not work yet.
 */
export function mountPanels(_config: StampedConfig): void {}
