{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    // the driver's page.evaluate callbacks execute in the browser; DOM
    // types exist for those closures only
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src"]
}
