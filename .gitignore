# dependencies
node_modules/

# build outputs
dist/
.next/
.turbo/
*.tsbuildinfo
apps/manager/src-tauri/target/
apps/web/next-env.d.ts
apps/web/.vercel/

# ci-assembled sidecar tree (release-macos.yml); only the README is committed
apps/manager/src-tauri/sidecar/*
!apps/manager/src-tauri/sidecar/README.md

# test artifacts
test-results/
playwright-report/
tests/.tmp/

# runtime state never lives in the repo
.mortal/

# misc
.DS_Store
*.log
.env
.env.*
# the box's template carries no secrets, only the names of them
!infra/box/.env.example
artifacts/demo/
